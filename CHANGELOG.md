# ЖУРНАЛ ИЗМЕНЕНИЙ - КАРТА ЖИЗНИ

---

### [Версия 1.0.0-альфа] - 17/01/2026

#### Добавлено

- Базовая архитектура проекта

- Графический интерфейс на PyQt6

- Интерактивная схема с узлами и связями

- Система базы данных SQLite с шифрованием

- Поддержка темной/светлой темы

- Контекстные меню для управления узлами

#### Изменено

- Начальная структура проекта

#### Исправлено

- Инициализация проекта

#### Технические детали

- Python 3.9+

- PyQt6 для GUI

- Шифрование AES-256

- Лицензионное соглашение для альфа-версии

#### Известные проблемы

- Редактор контента еще не реализован

- Нет системы напоминаний

- Нет экспорта в PDF

- Нет поиска по карте

#### Планы на следующую версию

1\. Редактор контента с вкладками

2\. Списки дел с галочками

3\. Система дат и напоминаний

4\. Прикрепление файлов

### [Версия 1.0.1-альфа] - 21/01/2026

1\.  Исправлено создание дочернего узла через пкм (с багами, связь не создается, сам узел сохраняется)

2\. Исправлено удаление узла

3\. Избавился от шлейфа при перетаскивании узла

4\. Отрисована сетка сцены

5\. Доработка тем (изменение цвета сцены)

6\. Изменение цвета рамки узла со связкой с цветом узла, изменение цвета подсветки узла.

#### Планы на следующую версию

1\. Исправление багов

2\. Запрет на создание отдельного узла

3\. Запрет на удаление/редактирование первоначального узла


### [Версия 1.0.2-альфа] - 23/01/2026

1\. Исправлена ошибка создания связи между узлами

### [Версия 1.0.3-альфа] - 24/01/2026

1\. Исправлен баг при удалении связи узла

2\. Исключена функция выделения и удаления связи

3\. Автоматизировано удаление связей удаляемого узла. 

#### Планы на следующую версию

1\. Исправление бага: при удалении дочернего конечного узла все корректно, если удаляется узел, который является и дочерним и родительским программа вылетает

2\. Нужно добавить автоматическое удаление всех последующих связей и узлов

### [Версия 1.0.4-альфа] - 24/01/2026

1\. Баг при удалении родительско-дочернего узла устранен.

2\. Реализовано автоматическое удаление дочерних связей и узлов при удалении узла

### [Версия 1.0.5-альфа] - 25/01/2026

1\. EdgeItem (связи): Исправлен boundingRect() и метод paint()

2\. NodeItem (узлы): Настроена отрисовка без видимых границ

3\. GraphView (просмотр): Удалены рамки, оптимизирована отрисовка

4\. Графическая сцена: Исправлен метод drawBackground()

5\. Несогласованные цвета в разных компонентах интерфейса. Реализована единая система управления темами для всего приложения

6\. Добавлено кэширование для графических элементов

7\. Использование prepareGeometryChange() для динамических объектов

8\. Оптимизированы методы отрисовки

### [Версия 1.0.6-альфа] - 28/01/2026

1\. Изменение направления схемы

2\. Создание дочерних узлов теперь снизу, а не справа

3\. Связи теперь вертикальные от нижней грани к верхней узлов

4\. Изменение направления стрелки (вниз)

### [Версия 1.0.7-альфа] - 30/01/2026

1\. Исправлен баг взаимодействия. Двигались элементы при перетаскивании, а не вся сцена. Перенос методов в Graphview

### [Версия 1.0.8-альфа] - 31/01/2026

Начало добавления функции содержимого узла. Этап 0.


1\. Добавлены константы типов вкладок

2\. Добавлена модель ContentTab

3\. Привязка списков вкладок к NodeContent

4\. Гарантировано наличие NodeContent у узла

### [Версия 1.0.9-альфа] - 01/02/2026

Содержимое узла. Этап 1.

1\. Исправлено сохранение содержимого узла: теперь используется корректный вызов commit вместо несуществующего метода save_data

2\. Импортирован QMenu из PyQt6.QtWidgets для поддержки меню вкладок

3\. Начата реализация класса NodeContentEditorDialog для редактирования содержимого узла

4\. Добавлены элементы интерфейса: кнопка редактирования названия, кнопка и меню добавления вкладок

5\. Подготовлена структура для будущей работы с вкладками содержимого (текст, файлы, списки и т.д.)

### [Версия 1.0.10-альфа] - 02/02/2026

Содержимое узла. Этап 1.1 (Частично этап 2)

1\. Реализован функционал редактирования названия узла - кнопка для переключения в режим редактирования названия

2\. Добавлена система вкладок в редакторе узла - возможность добавления вкладок разных типов (Текст, Файлы, Список, Список дел, Даты) (Из этапа 2)

3\. Создан интерфейс для управления вкладками - кнопка "+" с меню для добавления новых вкладок

4\. Интегрировано сохранение изменений названия узла в базу данных - обновление названия в БД и в сцене

5\. Исправлен метод add_child_node - устранено дублирование кода и улучшена обработка ошибок

6\. Улучшена структура UI редактора - разделение на шапку с названием и панель вкладок

##### Остаток на этап 2:

1\. Реализовать сохранение созданных вкладок

2\. Реализовать удаление вкладок с помощью ПКМ по вкладке

### [Версия 1.0.11-альфа] - 03/02/2026

1\. Реализовано удаление вкладок содержимого узла

2\. Реализована система сохранения и загрузки вкладок в содержимом узла (теперь вкладки не исчезают при повторном открытии)

3\.Обновлена структура базы данных для поддержки сохранения содержимого вкладок
4\. Исправлена ошибка в диалоге подтверждения удаления вкладки

5\. Исправлена ошибка при открытии контекстного меню вкладок в редакторе узла

### [Версия 1.0.12-альфа] - 05/02/2026

1\. Создан класс для виджета вкладки с текстом

2\. Исправление логики сохранения данных во вкладке

3\. Добавлено автосохранение при закрытии редактора узла

4\. Добавлен класс с базовым контрактом для редактора

5\. Изменение размеров узла

6\. Добавлен мягкий магнетизм к сетке с автоприлипанием для удобной сортировки/расположения узлов

### [Версия 1.0.13-альфа] - 06/02/2026

1\. Завершена базовая логика вкладки «Текст»

2\. Реализовано сохранение текста в модель узла

3\. Добавлено отслеживание изменений (вкладка понимает, что она изменена)

4\. Исправлены проблемы с пропаданием текста при закрытии редактора

5\. Настроено сохранение при закрытии редактора, при переключении вкладок

6\. Устранены ошибки, связанные с повторным сохранением

7\. Добавлена отладка, подтверждающая корректную работу логики

8\. Приведен в порядок метод сохранения (убрана устаревшая логика)

9\. Подтверждено, что двойной вызов сохранения при закрытии — нормальное поведение Qt

#### ЭТАП 3 фактически доведен до стабильного состояния

### [Версия 1.0.14-альфа] - 07/02/2026

1\. Реализована поддержка подтверждения изменения имени узла клавишей Enter

2\. Добавлена полная функция переименования вкладок (двойной клик и контекстное меню)

3\. Реализовано сохранение вкладок при потере фокуса и при нажатии Enter

4\. Кнопка карандаша для редактирования имени узла теперь работает как переключатель (открыть/закрыть режим редактирования)

5\. Введена система сохранения выбора темы в конфигурационный файл (data/config.json)

6\. Исправлена проблема с неправильной темой при первом открытии программы

7\. Тема приложения теперь сохраняется, загружается при каждом открытии и применяется при инициализации

8\. Удалены дублирующиеся жесткие стили из main.py, все управление темой централизовано в ui_main_window.py

#### Улучшения UX

- Редактирование названий узлов и вкладок стало интуитивнее и быстрее
- Пользовательские предпочтения по теме сохраняются между сеансами
- Светлая тема применяется корректно при первом открытии

### [Версия 1.0.15-альфа] - 08/02/2026

1\. Полностью реализована вкладка «Список»

2\. Добавлен интерфейс управления пунктами списка

3\. Исправлена проблема с отображением кнопок управления

4\. Реализовано добавление и удаление пунктов

5\. Включен drag & drop для изменения порядка элементов

6\. Настроено автосохранение списка: при переключении вкладок; при закрытии редактора

7\. Данные списка корректно восстанавливаются при повторном открытии узла

#### ЭТАП 4 завершен и стабилен

### [Версия 1.0.16-альфа] - 09/02/2026

1\. Выполнен аккуратный рефактор BaseTabWidget без изменения поведения

2\. Упрощена логика переключения вкладок

3\. Повышена готовность редактора к новым типам вкладок

4\. Код приведен в более устойчивое и расширяемое состояние

### [Версия 1.0.17-альфа] - 10/02/2026

1\. Реализована новая вкладка «Список дел (TODO)»

2\. Добавлена возможность создавать задачи с чекбоксами

3\. Реализовано редактирование текста задач прямо в списке

4\. Настроено автоматическое сохранение состояния задач

5\. Исправлены ошибки, связанные с особенностями PyQt6

6\. Приведен внешний вид кнопок TODO к единому стилю со вкладкой «Список»

7\. Исправлено отображение чекбоксов при добавлении новых задач

8\. Подтверждена корректная загрузка данных при повторном открытии узла

9\. Дополнены методы save_to_model в виджетах других вкладок под общий принцип

#### ЭТАП 5 завершен и стабилен

### [Версия 1.0.18-альфа] - 11/02/2026

1\. Реализована новая вкладка «Даты»

2\. Реализовано корректное завершение редактирования названия события по клавише Enter (во вкладке дат).

3\. Исправлена проблема с переходом фокуса на редактирование названия узла после нажатия Enter.

4\. Приведено поведение редактирования событий к единому стилю с вкладками «Список» и «Список дел».

5\. Разобрана и исправлена проблема применения темы к редактору узла.

6\. Обеспечена корректная синхронизация светлой и темной темы для окна редактора.

7\. Проведена проверка механизма наследования палитры и стилей в диалоговых окнах.

8\. Убрана черная полоса над кнопкой «Добавить дату»

- Настроены отступы и фон области прокрутки — визуальный дефект полностью исчез.

9\. Решена проблема с фоном на вкладке «Даты»

- Фон области событий и самих строк теперь правильно меняется при переключении темы.

- Устранена ситуация, когда фон оставался темным в светлой теме или светлым в темной.

10\. Устранены мелкие недочеты

- Исправлены опечатки в именах переменных, влиявшие на сохранение данных.

- Добавлены отладочные сообщения для удобства дальнейшей разработки.

#### ЭТАП 6 завершен и стабилен

### [Версия 1.0.19-альфа] - 12/02/2026

1\. Исправлены стрелки переключения месяцев.

2\. Исправлен цвет чисел дат в светлой теме.

3\. Выровнен фон календаря и строки дней недели.

4\. Убраны визуальные артефакты Fusion-стиля.

5\. Приведен к единому стилю диалог подтверждения удаления вкладки/узла (QMessageBox) для обеих тем.

6\. Общая визуальная целостность редактора узла приведена в соответствие с темами приложения.

### [Версия 1.0.20-альфа] - 13/02/2026

1\. Создана вкладка «Файлы»

2\. Реализован полноценный интерфейс для прикрепления файлов к узлу.

3\. Добавлена возможность добавлять файлы через кнопку «Добавить» и перетаскиванием (drag & drop).

4\. Файлы автоматически копируются в специальную папку данных (структура data/attachments/node_<id>/), оригиналы остаются на месте.

5\. В списке отображается имя файла, его размер и иконка.

6\. Реализовано открытие файла двойным кликом через стандартную программу.

7\. Безопасное удаление файлов

- При удалении файла из списка появляется диалог подтверждения с предупреждением об удалении с диска.

- При подтверждении файл физически удаляется из папки данных, после чего исчезает из списка.

- При удалении целой вкладки «Файлы» (через контекстное меню) все ее файлы также удаляются с диска автоматически.

8\. Безопасное удаление файлов

- При удалении файла из списка появляется диалог подтверждения с предупреждением об удалении с диска.

- При подтверждении файл физически удаляется из папки данных, после чего исчезает из списка.

- При удалении целой вкладки «Файлы» (через контекстное меню) все ее файлы также удаляются с диска автоматически.

9\. Исправлены технические ошибки

- Устранена проблема с отсутствием node_id во вкладке «Файлы».

- Исправлены конфликты типов данных при работе с путями файлов (WindowsPath → строка).

- Удалены дублирующие вызовы и лишний код.

10\. Реализован drag & drop во вкладке списка дел 

#### ЭТАП 7 завершен и стабилен

### [Версия 1.0.21-альфа] - 14/02/2026

1\. Создан файл app.py для централизованного запуска приложения

2\.Упрощен main.py — теперь он отвечает только за старт

3\. Убраны лишние альтернативные точки входа

4\. Bootstrap приложения стал чище и понятнее

5\. Создан отдельный модуль core/file_service.py

6\. Вынесена логика:

- Создания папок узлов

- Копирования файлов

- Удаления файлов

- Проверки существования файлов

7\. UI больше не работает напрямую с os и shutil

8\. Устранена прямая зависимость интерфейса от файловой системы

9\. Реализовано автоматическое удаление папки attachments/node_<id>
    При рекурсивном удалении узла удаляются:

- Сам узел

- Все потомки

- Все файлы

- Все папки

10\. Устранены «висячие» папки после удаления

11\. Архитектурные улучшения:

- Начато отделение UI от логики хранения данных

- Введен первый сервисный слой (FileService)

- Снижена связанность MainWindow

- Подготовлена база для дальнейшего разделения на core / ui / widgets

### [Версия 1.0.22-альфа] - 15/02/2026

#### Архитектура
- Введен сервисный слой `ContentService` для управления содержимым узлов
- Начато разделение бизнес-логики и моделей данных
- Подготовлена база для внедрения Repository-слоя
- Архитектура переведена на схему: `UI → Service → Model`

#### Node Content
- Реализовано добавление вкладок через сервисный слой
- Реализовано удаление вкладок через сервисный слой
- Обновлена логика получения и перемещения вкладок
- Исключено прямое управление `node.content.tabs` из UI

#### Модели данных
- Удалена бизнес-логика из `NodeContent`
- Удален метод `save()` из модели
- Модель приведена к формату «чистая структура данных»

#### Исправления
- Исправлены ошибки вызова методов сервиса
- Устранено двойное добавление вкладок
- Исправлены ошибки удаления вкладок

#### Дополнительно
- Чистка файлов от старого закомментированного кода

### [Версия 1.0.23-альфа] - 17/02/2026

#### Интеграция сервисного слоя для работы с вкладками
- Внедрен ContentService для операций над вкладками (добавление, удаление, переименование, обновление данных).
- Внедрен ContentRepository для сохранения контента узла в БД.

#### Модернизация виджетов вкладок
- Базовый класс BaseTabWidget теперь принимает node_content и tab.
- Все виджеты-наследники (TextTabWidget, ListTabWidget, TodoTabWidget, DatesTabWidget, FilesTabWidget) обновлены для работы с новым конструктором.
- В методах save_to_model каждого виджета прямое присвоение tab.data заменено на вызов ContentService.update_tab_data().

#### Исправление ошибок в диалоге редактора узла (NodeContentEditorDialog)
- Метод create_tab_widget исправлен для корректной передачи двух аргументов (node_content, tab) во все виджеты.
- У конструктора FilesTabWidget убран лишний параметр node_id (теперь node_id берется из node_content).
- В методе finish_rename_tab добавлен вызов ContentService.rename_tab для сохранения нового названия вкладки через сервис.

#### Устранение дублирования кода
- Удален дублирующийся метод add_child_node (второй экземпляр после closeEvent). Оставлена рабочая версия с отладочной печатью.


### [Версия 1.0.24-альфа] - 18/02/2026

#### Вынос диалога редактора узла в отдельный модуль
- Создан файл `ui/editor_dialog.py`, в который перенесены все классы, связанные с редактированием узла: `TitleEditField`, `TabRenameEditField`, `BaseTabWidget`, `TextTabWidget`, `ListTabWidget`, `TodoTabWidget`, `DatesTabWidget`, `DatesContainer`, `FilesTabWidget`, `NodeContentEditorDialog`.
- В `ui_main_window.py` добавлен импорт `NodeContentEditorDialog` из нового модуля, сам файл очищен от перенесенных классов.
- В конструктор `NodeContentEditorDialog` добавлен параметр `main_window` для передачи ссылки на главное окно, что позволило избавиться от прямого обращения к родителю и избежать циклических зависимостей.
- В методе `get_main_window` заменена проверка на тип `MainWindow` (который не импортирован) на проверку наличия характерных атрибутов (`dark_mode` и `scene`), что устранило ошибку `NameError`.

#### Исправление повторного показа диалога пароля
- Обнаружен и удален дублирующийся код в методе `show_login_dialog`, который создавал второй экземпляр диалога и вызывал его повторно.
- Добавлен флаг `_login_shown` для предотвращения возможных повторных вызовов в будущем.

#### Исправление позиционирования новых узлов
- Методы `add_root_node`, `add_node_at_position`, `add_child_node` доработаны для создания узлов с привязкой к сетке (шаг 50 пикселей).
- В класс `GraphScene` добавлен атрибут `GRID_SIZE = 50`.
- В `add_root_node` теперь используется последняя созданная позиция (хранится в `last_node_pos`) с отступом на одну ячейку вниз; при отсутствии узлов новый узел помещается в центр экрана с привязкой к сетке.
- В `add_child_node` позиция дочернего узла рассчитывается строго на две ячейки ниже родителя, игнорируя координаты мыши; устранено дублирование вызовов `add_node`.
- Исправлена ошибка `AttributeError: 'GraphScene' object has no attribute 'grid_size'` – теперь правильно используется `self.scene.GRID_SIZE`.

#### Улучшение стабильности при удалении узлов
- После удаления узла позиция `last_node_pos` остается корректной, что позволяет новым узлам создаваться под последней использованной позицией, даже если последний узел был удален.

#### Результат
- Программа стабильно запускается из терминала (`python main.py`), диалог пароля показывается один раз, редактор узла открывается без ошибок, все типы вкладок работают.
- Создание узлов происходит с четкой привязкой к сетке, дочерние узлы автоматически выстраиваются под родителями, что улучшает визуальную организацию карты.
- Реализован этап 4 рефакторинга архитектуры.

### [Версия 1.0.25-альфа] - 20/02/2026

#### Разделение виджетов вкладок по отдельным файлам
- Создана папка `widgets` с файлом `__init__.py` для организации пакета.
- Класс `BaseTabWidget` вынесен в отдельный файл `widgets/base_tab.py`.
- Класс `TextTabWidget` вынесен в `widgets/text_tab.py`.
- Класс `ListTabWidget` вынесен в `widgets/list_tab.py`.
- Класс `TodoTabWidget` вынесен в `widgets/todo_tab.py`.
- Классы `DatesTabWidget` и `DatesContainer` вынесены в `widgets/date_tab.py`.
- Класс `FilesTabWidget` вынесен в `widgets/files_tab.py`.
- Во всех вынесенных файлах добавлены корректные импорты (PyQt6, сервисы, модели) и удалены внутренние импорты, вызывавшие зависимости.

#### Очистка `ui/editor_dialog.py`
- Из `editor_dialog.py` удалены все перенесенные классы виджетов.
- Добавлены импорты из пакета `widgets` для `BaseTabWidget` и всех конкретных виджетов.
- Файл теперь содержит только классы `TitleEditField`, `TabRenameEditField` и `NodeContentEditorDialog`, что соответствует целевой архитектуре.

#### Проверка работоспособности
- После реорганизации приложение успешно запускается без ошибок импорта.
- Все типы вкладок (текст, список, TODO, даты, файлы) корректно загружаются и сохраняют данные.
- Функциональность редактора узла полностью сохранена.

#### Результат
- Достигнута целевая структура папок, где все виджеты вкладок изолированы в модулях пакета `widgets`.
- Упрощена навигация по коду и улучшена поддержка проекта.

### [Версия 1.0.26-альфа] - 21/02/2026

#### Вынос управления темами в отдельный модуль
- Создан файл `ui/themes.py` с функциями `set_dark_mode()`, `is_dark_mode()` и `get_stylesheet()` для централизованного управления состоянием темы.
- Виджеты (например, `date_tab.py`) теперь получают состояние темы через `is_dark_mode()` вместо обращения к `self.window().dark_mode`.

#### Перенос стилей в отдельные .qss файлы
- Созданы файлы `resources/styles/light.qss` и `resources/styles/dark.qss`.
- Все стили для главного окна, диалога редактора узла, календаря, QMessageBox и других элементов перенесены из кода в соответствующие .qss файлы.
- В `MainWindow.apply_theme` теперь используется `get_stylesheet()` для загрузки актуального файла стилей.

#### Рефакторинг методов применения темы
- Метод `apply_theme` в `editor_dialog.py` упрощен: теперь он только обновляет вкладки через `refresh_theme()` и не устанавливает палитру или стили (они наследуются глобально).
- Удален неиспользуемый метод `update_all_dialogs_theme` из `MainWindow`, исправлена ошибка `AttributeError`.
- Метод `_apply_theme_to_widget` в `date_tab.py` очищен от зависимостей от главного окна, теперь использует `is_dark_mode()`.

#### Улучшение визуального выделения строк в списках
- В .qss файлы добавлены правила для `QListWidget::item:selected`, задающие цвет фона выделенных элементов: светло-серый (`#e0e0e0`) для светлой темы, темно-серый (`#404040`) для темной.
- Убрана пунктирная рамка фокуса (`outline: none`) для списков, что устранило "драцкий синий" и кривую рамку.

#### Результат
- Тема применяется единообразно ко всем элементам приложения.
- Виджеты не зависят от главного окна, код стал чище.
- Исправлен давний визуальный баг с выделением строк в списках.

### [Версия 1.0.27-альфа] - 22/02/2026

#### Создание сервисного слоя для работы с графом
- Создан файл `core/graph_service.py`, инкапсулирующий все операции с узлами и ребрами (добавление, получение, обновление, удаление).
- В `GraphService` вынесены методы: `add_node`, `add_child_node`, `get_node`, `get_all_nodes`, `get_children`, `get_all_descendants`, `update_node_position`, `update_node_color`, `toggle_node_collapsed`, `delete_node`, а также методы для работы с ребрами (`add_edge`, `get_all_edges`, `get_edges_for_node`, `delete_edge`).
- Добавлен метод `commit()` для фиксации транзакций и `close()` для закрытия сессии БД, что полностью скрывает прямой доступ к `db_session` из главного окна.

#### Интеграция GraphService в MainWindow
- В конструктор `MainWindow` удалено прямое создание `graph_service` – теперь он инициализируется в методе `init_database` после успешного подключения к БД.
- Все методы главного окна, работающие с узлами и ребрами (`load_data`, `add_root_node`, `add_node_at_position`, `add_child_node`, `delete_node`, `delete_edge`, `update_node_position`, `update_node_color`, `toggle_node_collapsed`, `open_node_editor`), переписаны для использования `graph_service` вместо прямых вызовов `db_session`.
- Метод `save_data` адаптирован для использования `graph_service.commit()`.
- В `closeEvent` добавлен вызов `graph_service.close()` для корректного закрытия сессии.

#### Исправление ошибок инициализации
- Устранена ошибка `'NoneType' object has no attribute 'get_all_nodes'`, возникавшая из-за попытки создать `GraphService` до инициализации `db_session`. Теперь сервис создается только после успешного подключения к БД.
- Добавлены проверки наличия `graph_service` в методах, вызываемых до его создания (например, `autosave` и `closeEvent`), что предотвращает возможные сбои.

#### Прочее
- Заполнены файлы `__init__.py` для удобства, сжатия, аккуратности импортов методов и классов. 
- Скорректированы импорты в `ui_main_window.py`, `graph_service.py`, `editor_dialog.py`

#### Результат
- Главное окно больше не имеет прямых зависимостей от моделей БД и сессии – вся логика работы с графом инкапсулирована в сервисе.
- Код стал чище, легче для тестирования и дальнейшего расширения (например, добавления истории изменений, операций с выделенными узлами).
- Приложение стабильно запускается и работает, все функции (создание, удаление, перемещение узлов, редактирование контента) сохранены.

### [Версия 1.0.28-альфа] - 23/02/2026

#### Приведение файлов под планируемую структуру
- У файлов `ui_edge_item.py`, `ui_graph_scene.py`, `ui_main_window.py`, `ui_node_item.py` убраны префиксы "ui_".
- Файлы `edge_item.py`, `graph_scene.py`, `main_window.py`, `node_item.py` перенесены в папку "ui".

#### Корректировка импортов
- Исправлены импорты во всех необходимых файлов после изменения названий файлов и их расположения.
- Импорты приведены к единому виду во избежание ошибок "from `folder.file` import class|method"

#### Исправления и доработка
- Устранена ошибка рекурсивного удаления узла. Изменен устаревший метод `db_session.get_edges_from_node` на `db_session.get_node_edges`
- В метод `add_file` добавлен вызов `save_to_model`, что сохраняет данные в модель сразу после добавления файла.
- Исправление `add_file` устранило баг: при удалении вкладки без перехода на другую или переоткрытии узла файлы не удалялись из *\data\attachments\node_id*

### [Версия 1.0.29-альфа] - 25/02/2026

#### Унификация заголовков файлов проекта
- Во все ключевые файлы проекта добавлены единообразные заголовки, содержащие:
  - Краткое описание назначения файла.
  - Полный блок с информацией о проекте, авторе, лицензии и конфиденциальности.
- Охвачены следующие модули и директории:
  - Корневые файлы: `main.py`, `app.py`.
  - Ядро (`core/`): `database.py`, `models.py`, `graph_service.py`, `content_service.py`, `content_repository.py`, `file_service.py`.
  - Пользовательский интерфейс (`ui/`): `main_window.py`, `editor_dialog.py`, `graph_scene.py`, `node_item.py`, `edge_item.py`, `themes.py`.
  - Виджеты (`widgets/`): `base_tab.py`, `text_tab.py`, `list_tab.py`, `todo_tab.py`, `date_tab.py`, `files_tab.py`.

#### Результат
- Все файлы проекта теперь имеют стандартизированное оформление заголовков, что улучшает читаемость и упрощает навигацию по коду.
- Повышена согласованность стиля и информации об авторских правах.

### [Версия 1.0.30-альфа] - 26/02/2026, 28/02/2026

#### Docstring энд другие улучшения полотна по файлам:
- core/
  - database.py - уже содержит строки документации
  - models.py - не требует дополнений
  - graph_service.py - уже содержит строки документации
  - content_service.py - уже содержит строки документации
  - content_repository.py - уже содержит строки документации
  - file_service.py - добавлены строки документации ко всем методам

- ui/
  - main_window.py - скорректированы комментарии, удален ненужный закомментированный код.
  - editor_dialog.py - скорректированы комментарии, удален ненужный закомментированный код, добавлены строки документации к некоторым методам.
  - graph_scene.py - скорректированы комментарии, удален ненужный закомментированный код.
  - node_item.py - уже содержит строки документации
  - edge_item.py - скорректированы комментарии, удален ненужный закомментированный код.
  - themes.py - уже содержит строки документации.

- widgets/
  - base_tab.py - добавлены строки документации.
  - text_tab.py - удален ненужный закомментированный код, добавлены строки документации, скорректированы комментарии.
  - list_tab.py - удален ненужный закомментированный код.
  - todo_tab.py - удален ненужный закомментированный код, добавлены строки документации, скорректированы комментарии.
  - date_tab.py - удален ненужный закомментированный код, добавлены строки документации.
  - files_tab.py - добавлены строки документации, скорректированы комментарии, удален ненужный закомментированный код.

#### Документация проекта
- Полностью обновлен файл `ARCHITECTURE.md` под архитектуру после рефакторинга. Добавлен заголовок файла для единообразия.
- Добавлен файл `ARCHITECTURE_STRICT.md` - технический документ строгий.
- Добавлен файл `DEPENDENCY_DIAGRAM.md` - диаграмма в формате Mermaid.

#### Рефакторинг завершен 🥳

---

***Этот журнал ведется для внутреннего использования в процессе разработки.***

